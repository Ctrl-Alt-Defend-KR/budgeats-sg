# 런타임 의존성
# 새 라이브러리 추가 시 CI 의존성 취약점 스캔이 돈다는 점을 고려할 것 (CLAUDE.md 9절)

fastapi
uvicorn[standard]
pydantic
pydantic-settings
sqlalchemy
alembic
psycopg[binary]

# Places API 호출용 HTTP 클라이언트 (app/services/places 에서만 사용)
httpx

# Google OAuth 2.0 및 세션 쿠키 서명
authlib
itsdangerous
